# سجل التغييرات (CHANGELOG)

## [1.3.0] - 2026-08-06

### 🎨 إحياء الكنز الصوتي والبصري والمؤثرات (Sprint 5)
- **سكربت تشريح الأسطوانة (`scripts/extract_all_letters.dart`)**: سكربت لاستخراج وفك ضغط الملفات الصوتية المسجلة احترافياً في ملفات EXE للـ 27 حرفاً.
- **مولّد المؤثرات الصوتية للواجهة (`AudioGenerator`)**: مولّد برمجيات لمؤثرات WAV الحية (الإجابة الصحيحة، الخاطئة، الاحتفال، النقر، والـ Streak) بدون مكتبات خارجية.
- **الفهرس المركزي للأصول البصرية (`AppImages`)**: خريطة ترجمة وفهرسة شاملة لربط كافة الحروف الـ 28 وأيقونات الواجهة بالأصول البصرية.
- **دمج الصور بالواجهات مع Fallback ذكي**: دعم الصور في الصقر الرئاسي (`FalconMascot`) وشاشة البداية (`SplashScreen`) والمسار البصري (`AdventurePath`) وشاشة التعليم (`LetterIntroScreen`) مع السقوط التلقائي السلس في حال التغيب.

## [1.2.0] - 2026-08-06

### 🗺️ مسار المغامرة البصري وشخصية الصقر (Sprint 4: Desert Oasis Path Map)
- **خريطة المسار التفاعلية (`AdventurePath` & `PathPainter`)**: تحويل القائمة النصية السابقة إلى مسار مغامرة بصري متعرج (Serpentine Zigzag Map) مكسو بالألوان التفاعلية ومؤشرات الإتقان/القفل مع تموجات بزييه.
- **شخصية الصقر (`Falcon Mascot`)**: دمج شخصية الصقر كـ Mascot متحرك (Floating sine-wave animation) يتفاعل بحسب حالة الطفل (Happy, Encouraging, Thinking, Celebrating) مع فقاعات نصية تشجيعية في الشاشة الرئيسية وشاشة ختام الجلسة.
- **لوحة الوالدين (`ParentsScreen`)**: شاشة مخصصة للوالدين تتيح تتبع نسبة إنجاز الطفل في كل حرف من الحروف الـ 28 مع شريط إحصائيات عام وخيار إمكانية إعادة ضبط البيانات بالكامل.
- **التحية الزمنية الذكية**: إضافة تحية تلقائية في أعلى الشاشة الرئيسية تتغير بحسب وقت اليوم (صباح الخير، مرحباً، مساء الخير).

## [1.1.0] - 2026-08-06

### 🚀 الميزات الجديدة (Gamification & UI Polish)
- **نظام نقاط الخبرة (XP System)**: حساب وتراكم نقاط XP المكتسبة من كل جلسة وتخزينها محلياً مع حساب المستوى الحالي للطفل.
- **نظام الأيام المتتالية (Streak System)**: تتبع الأيام المتتالية واستمرار نشاط الطفل اليومي.
- **لوحة الأوسمة والشارات (Badges System)**: فتح 10 أنواع من الشارات والأوسمة التكريمية بناءً على الإنجازات وسرعة الإجابة والتفوق.
- **شاشة الأوسمة (`BadgesScreen`)**: عرض كافة الأوسمة المفتوحة والمقفلة مع متطلبات كل وسام في شبكة بصرية.
- **أنيميشن الاحتفال (`RewardAnimation`)**: أنيميشن confetti بالجسيمات الملونة عند فتح شارة أو إكمال مهارة.
- **عدّاد XP المتحرك (`XpCounterAnimation`)**: أنيميشن صاعد لـ XP بدقة وسلاسة في شاشة الانتهاء.
- **شاشة البداية الاحترافية (`SplashScreen`)**: أنيميشن ترحيبي مع شريط تحميل وتوجيه ذكي.
- **مؤثرات صوتية للواجهة (`TtsService.playUiSound`)**: دعم أصوات الإجابة الصحيحة، الخاطئة، والمكافآت.

### 🧪 الاختبارات والجودة
- إضافة اختبارات عنصر مسار المغامرة `AdventurePath`.
- إضافة اختبارات وحدة لـ `MasteryService` (تغطية 100% للمنطق الأساسي و XP والشارات).
- إضافة اختبارات تماسك وتكامل لـ `SkillDAG`.
- إضافة اختبارات وحدة لـ `TtsService`.
- اجتياز `flutter analyze` بـ 0 أخطاء و 0 تحذيرات.
- اجتياز 100% من اختبارات Dart/Flutter.
